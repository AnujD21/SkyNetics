# SkyNetics — Python Dependencies
# Install with: pip install -r requirements.txt

# ── AI / Computer Vision ──────────────────────
ultralytics>=8.0.0          # YOLOv8 inference & training
opencv-python>=4.8.0        # Image/video processing
Pillow>=10.0.0              # Image utilities
torch>=2.0.0                # PyTorch (CUDA build for Jetson)
torchvision>=0.15.0

# ── Signal Processing ────────────────────────
numpy>=1.24.0
scipy>=1.10.0               # RF triangulation math

# ── Drone / MAVLink ──────────────────────────
dronekit>=2.9.2             # High-level drone control
pymavlink>=2.4.40           # MAVLink protocol
pyserial>=3.5               # Serial interface for RF module

# ── Thermal Imaging ──────────────────────────
# For FLIR Lepton: install PureThermal SDK separately
# mlx90640-python           # Uncomment if using MLX90640

# ── Ground Control API ───────────────────────
fastapi>=0.100.0
uvicorn>=0.23.0
websockets>=11.0

# ── Visualization ────────────────────────────
folium>=0.14.0              # Interactive GPS heatmaps
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Configuration & Utilities ────────────────
pyyaml>=6.0                 # config.yaml parsing
python-dotenv>=1.0.0
loguru>=0.7.0               # Structured logging
tqdm>=4.65.0                # Progress bars

# ── Testing ──────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0
